apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fuh-realtime-emotions.fullname" . }}-test-connection"
  labels:
    {{- include "fuh-realtime-emotions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fuh-realtime-emotions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
