apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuh-realtime-emotions.fullname" . }}
  labels:
    {{- include "fuh-realtime-emotions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuh-realtime-emotions.selectorLabels" . | nindent 4 }}
