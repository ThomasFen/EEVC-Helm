{{- if and .Values.emotionbackend.ingress.enabled .Values.authentication.ingress.enabled .Values.jitsi.web.ingress.enabled }}
1. Trust certificates of following URLs and make sure they resolve to your k8 ip.
   e.g modify /etc/hosts or use something like the minikube addon ingress-dns. 
     http{{ if $.Values.emotionbackend.ingress.tls }}s{{ end }}://{{ .Values.emotionbackend.ingress.hostname }}
     http{{ if $.Values.authentication.ingress.tls }}s{{ end }}://{{ .Values.authentication.ingress.hostname }}
{{- with (first .Values.jitsi.web.ingress.tls) }}
    https://{{ first .hosts }}
{{- end }}
2. Use preconfigured login data to authenticate in Jitsi:
     Name: patient | physician | secondpatient | secondphysician etc.
     Password: patient | physician | secondpatient | secondphysician etc.   
3. During a Jitsi video conference, click on the 3 dots next to a patient or in the
   main toolbar and enable/disable emotion detection.
Note: Verify that jitsi.jvb.publicIP resolves to the public IP of the deployment.
{{- end }}

